package kr.hhplus.be.server.apps.order.domain.service;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;

@Service
@RequiredArgsConstructor
public class PointService {
//    private final UserRepository userRepository;
//
//    @Transactional
//    public User processPayment(Long userId, Integer paymentAmount) {
//        UserPoint userPoint = userRepository.findUserByUserId(userId);
//        user.setPoint(user.getPoint() - paymentAmount);
//
//        return userRepository.save(user); // 차감된 포인트를 저장
//    }
}
